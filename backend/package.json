{
  "name": "bcs-memory-box-trial",
  "version": "0.1.0",
  "description": "Backend service for the BCS Memory Box free-trial flow. Receives audio, transcribes via AssemblyAI, cleans up via Claude, and emails the polished sample back via Resend.",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
